# Tasks
- [x] Fix email timeout during account verification
- [x] Investigate why verification emails are not arriving (background fail)
- [x] Implement new `/basicauth` OIDC provider endpoint on `auth-server`
- [x] Implement new `/basicauth/.well-known/oauth-protected-resource` route on `mcp-server`
- [ ] Implement A2UI for `list_memories_ui` tool on `mcp-server`
